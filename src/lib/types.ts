import { LucideIcon } from 'lucide-react'

export interface IconCardData {
  icon: LucideIcon
  title: string
  description: string
}

export interface SectionHeaderData {
  label: string
  title: string
  subtitle: string
}